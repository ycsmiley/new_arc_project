# Contracts Environment Variables
# Copy this to .env: cp .env.example .env

# =============================================================================
# 部署相關配置
# =============================================================================

# 1. 部署錢包私鑰 (必須)
# 這個錢包將用來部署合約，需要有 ARC 測試幣來支付 gas
PRIVATE_KEY=0xYourPrivateKeyHere

# 如何獲取：
# - 從 MetaMask: 帳戶詳情 > 匯出私鑰
# - 建議創建一個新的測試錢包，不要使用主錢包
# ⚠️  警告：永遠不要洩露真實錢包的私鑰！

# 2. Aegis Server Wallet 地址 (必須)
# 這個地址將用於簽署融資請求（EIP-712 簽名）
# 建議：使用與後端 SERVER_WALLET_PRIVATE_KEY 對應的地址
AEGIS_SERVER_WALLET=0xYourAegisServerWalletAddress

# 如何獲取：
# - 後端 .env 中的 AEGIS_SERVER_WALLET 地址
# - 或從私鑰生成地址：
#   npx hardhat run scripts/get-address.js

# 3. Arc Testnet RPC URL (選填，有預設值)
ARC_TESTNET_RPC_URL=https://rpc.testnet.arc.network

# 4. 初始流動性 (選填，預設為 0)
# 部署時可以同時存入初始流動性，單位：USDC (6 decimals)
# 例如：1000000 = 1 USDC
INITIAL_LIQUIDITY=0

# =============================================================================
# 驗證相關 (選填)
# =============================================================================

# Arc Testnet Block Explorer API Key (如果有的話)
ETHERSCAN_API_KEY=YourArcScanApiKey

# =============================================================================
# Gas 報告 (選填)
# =============================================================================

# 是否顯示 gas 使用報告
REPORT_GAS=false
