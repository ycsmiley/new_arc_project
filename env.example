# =============================================================================
# AEGIS FINANCE - ENVIRONMENT VARIABLES TEMPLATE
# =============================================================================
# Copy this file to create your environment configurations:
#   - frontend/.env.local (for Next.js frontend)
#   - backend/.env (for Nest.js backend)
#   - contracts/.env (for Hardhat smart contracts)
# =============================================================================

# -----------------------------------------------------------------------------
# Frontend Environment Variables (Copy to: frontend/.env.local)
# -----------------------------------------------------------------------------

# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# WalletConnect Configuration
NEXT_PUBLIC_WALLET_CONNECT_PROJECT_ID=your_walletconnect_project_id

# Arc Smart Contract Address (deployed ArcPool.sol)
# Already deployed on Arc Testnet - you can use this address directly
NEXT_PUBLIC_ARC_CONTRACT_ADDRESS=0x8080900fD63d6C7e4E716D1cb65F1071e98cD14C

# Arc Testnet Configuration
NEXT_PUBLIC_ARC_RPC_URL=https://rpc.testnet.arc.network
NEXT_PUBLIC_ARC_CHAIN_ID=5042002

# Backend API URL
NEXT_PUBLIC_API_URL=http://localhost:3001

# -----------------------------------------------------------------------------
# Backend Environment Variables (Copy to: backend/.env)
# -----------------------------------------------------------------------------

# Server Configuration
PORT=3001
NODE_ENV=development

# Supabase Configuration (Backend)
SUPABASE_URL=your_supabase_project_url
SUPABASE_SERVICE_KEY=your_supabase_service_role_key

# JWT Secret (generate a random string)
JWT_SECRET=your_very_secure_jwt_secret_key_here

# Aegis Server Wallet (for signing financing requests)
# ⚠️  IMPORTANT: Keep your private key secret! Never commit the actual .env file
SERVER_WALLET_PRIVATE_KEY=0x...
# Public address corresponding to the private key above
AEGIS_SERVER_WALLET=0x782c3446aedabdd934e97ee255d5c5c62fe289d3

# Arc Testnet Configuration
ARC_RPC_URL=https://rpc.testnet.arc.network
# Already deployed on Arc Testnet - you can use this address directly
ARC_CONTRACT_ADDRESS=0x8080900fD63d6C7e4E716D1cb65F1071e98cD14C
ARC_CHAIN_ID=5042002

# Mistral AI (Optional - for AI-powered risk scoring)
# Get your free API key at: https://console.mistral.ai/
MISTRAL_API_KEY=your_mistral_api_key

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# Alchemy API (optional, for backup RPC)
ALCHEMY_API_KEY=your_alchemy_api_key

# -----------------------------------------------------------------------------
# Smart Contract Environment Variables (Copy to: contracts/.env)
# -----------------------------------------------------------------------------

# Deployment Private Key (for deploying contracts)
PRIVATE_KEY=0x...

# Arc Testnet RPC
ARC_TESTNET_RPC_URL=https://rpc.testnet.arc.network

# Aegis Server Wallet Address (for granting AEGIS_ROLE)
# This address will be granted AEGIS_ROLE to sign financing requests
AEGIS_SERVER_WALLET=0x782c3446aedabdd934e97ee255d5c5c62fe289d3

# Etherscan/Arc Explorer API Key (for contract verification)
ARC_EXPLORER_API_KEY=your_explorer_api_key

# Initial Liquidity (optional, in USDC with 6 decimals, e.g., 10000000000 = 10,000 USDC)
INITIAL_LIQUIDITY=0

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================

# 1. Frontend Setup:
#    cp env.example frontend/.env.local
#    Then edit frontend/.env.local with your values

# 2. Backend Setup:
#    cp env.example backend/.env
#    Then edit backend/.env with your values

# 3. Contracts Setup:
#    cp env.example contracts/.env
#    Then edit contracts/.env with your values

# =============================================================================
# DEPLOYED CONTRACT INFORMATION
# =============================================================================

# The following contract is already deployed and ready to use:
# Contract Address: 0x8080900fD63d6C7e4E716D1cb65F1071e98cD14C
# Network: Arc Testnet
# Deployed: 2025-11-08
# View on Explorer: https://explorer.testnet.arc.network/address/0x8080900fD63d6C7e4E716D1cb65F1071e98cD14C
#
# You can use this contract directly without deploying your own.
# If you want to deploy your own contract, see contracts/.env.example

# =============================================================================
# NOTES
# =============================================================================

# - NEVER commit actual .env files to git (they're in .gitignore)
# - Keep your private keys secure and never share them
# - For local development, you can use dummy values
# - For production, use proper secret management
# - Public information (contract addresses, RPC URLs) is safe to include in env.example

